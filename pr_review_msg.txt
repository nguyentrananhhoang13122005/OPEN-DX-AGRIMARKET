## 🤖 DX-AgriMarket AI Deep Review — PR #220

**Tổng quan:** Fix lỗi Authorization và Cache-miss cho API Thời tiết.
**Vòng review:** 10/10 hoàn thành
**Verdict:** REQUEST CHANGES

---

### 🔴 HIGH — Phải sửa trước khi merge

#### [H1] Vi phạm Hexagonal Architecture (Direct DB Access in Route Handler)
- **File:** `apps/web/src/app/api/weather/route.ts` | **Line:** 49
- **Vấn đề:** Trực tiếp import và gọi `prisma.parcel.findUnique` bên trong `route.ts`. Theo nguyên tắc kiến trúc (rules-and-limits.md §5), Route Handler KHÔNG được chứa business logic hay tương tác trực tiếp với Infrastructure (Prisma). Mọi thao tác check quyền (Authorization) với Database phải được đẩy vào trong UseCase (`GetWeatherUseCase`) hoặc thông qua một Interface/Port.
- **Code lỗi:**
  ```typescript
  import { prisma } from '@/infrastructure/db/prisma.client';
  // ...
  if (session.user.role === 'farmer') {
    const parcel = await prisma.parcel.findUnique({
      where: { id: validatedParcelId },
      include: { household: true },
    });
  // ...
  ```
- **Cách sửa:** Chuyển logic kiểm tra quyền (truyền `session.user` vào) xuống `GetWeatherUseCase.execute(parcelId, date, userSession)`. Trong UseCase, dùng `this.parcelPort.findById()` (cần update Port để trả về thông tin `household` nếu cần) để kiểm tra quyền sở hữu, thay vì gọi thẳng `prisma` ở route layer.

#### [H2] Thiếu Issue Link trong PR Description
- **File:** PR Metadata
- **Vấn đề:** PR Description đang hoàn toàn trống. Theo Conventional Commits và quy định tracking, PR phải link tới Issue.
- **Cách sửa:** Sửa lại description của PR trên GitHub, thêm dòng `Closes #127` để tự động link và đóng issue khi merge.

---

### ✅ Điểm tốt (Acknowledge good work)
- Test cases viết rất đầy đủ và cover tốt cả nhánh lỗi 403 lẫn nhánh Cache Miss 200 (Mocking Prisma tốt).
- CI/CD checks (Lint, Type Check, Tests, Build) đã xanh toàn bộ!
- Fix được triệt để vấn đề crash UI (DomainError văng ra ngoài) bằng cách graceful degradation trả về `null`.

---
*Review bởi: DX-AgriMarket AI (pr-deep-review skill) | MIT License | Copyright (c) 2026 Nguyễn Trần Anh Hoàng*
